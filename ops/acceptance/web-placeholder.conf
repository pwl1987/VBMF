# CP-01E software-acceptance placeholder: the Web Console is an explicitly
# deferred packet (CONTROL-PLANE-ENTRY-01 planning C13/§7). This server only
# satisfies the frozen Nginx route table upstream (B8: location / → web) with
# an honest static response so the ingress chain can come up in acceptance;
# it implements NO console functionality.
server {
    listen 5173;
    location / {
        default_type text/plain;
        return 200 "VBMF web console placeholder - console packet not yet provisioned\n";
    }
}
