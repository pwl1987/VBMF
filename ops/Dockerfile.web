# React Web UI (Vite + shadcn/ui) - dev server with HMR
# TODO(Phase 1): wire real build context once web/src exists.
# Build contract (Deployment SoT §4): Vite dev server + source bind mount + HMR.
FROM node:20-alpine
WORKDIR /app
# COPY package*.json ./
# RUN npm ci
# COPY . .
# CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
