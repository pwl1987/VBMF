id: FI-05-001
title: FI-05 HLS segment failure → RESTART_ADAPTER → alternate
gate: G-RUNTIME
reference: FI
fixture_id: F-FI-05-HLS-FAIL
env_prereq_id: ENV-LAB-01
runner: runners/run_fi_matrix.py
depends_on: [AC-01-001]
timeout: 120
preconditions:
  - "HLS Adapter active"
actions:
  - step: 1
    do: "Inject FI-05: HLS segment gen fail >=3 consecutive"
expected:
  restart_adapter_triggered: true
  alternate_active: true
  output_health_after: "HEALTHY"
pass_rule: "restart_adapter_triggered == true AND alternate_active == true AND output_health_after == 'HEALTHY'"
evidence:
  - "evidence/{id}_{run_ts}_fi_log.json"
artifact_naming: "{id}_{run_ts}_{pass|fail}.json"
retry_rule: {max: 1, backoff_s: 30}
abort_rule: {consecutive_fail: 3}
