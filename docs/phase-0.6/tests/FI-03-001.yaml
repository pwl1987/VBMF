id: FI-03-001
title: FI-03 Primary FFmpeg crash → RESTART + RESUME
gate: G-RUNTIME
reference: FI
fixture_id: F-FI-03-FFMPEG-CRASH
env_prereq_id: ENV-LAB-01
runner: runners/run_fi_matrix.py
depends_on: [AC-01-001]
timeout: 120
preconditions:
  - "Primary FFmpeg running"
actions:
  - step: 1
    do: "Inject FI-03: SIGKILL primary FFmpeg"
expected:
  restart_triggered: true
  no_pts_discontinuity: true
  pipeline_health_after: "HEALTHY"
pass_rule: "restart_triggered == true AND no_pts_discontinuity == true AND pipeline_health_after == 'HEALTHY'"
evidence:
  - "evidence/{id}_{run_ts}_fi_log.json"
artifact_naming: "{id}_{run_ts}_{pass|fail}.json"
retry_rule: {max: 1, backoff_s: 30}
abort_rule: {consecutive_fail: 3}
