id: HA-05-001
title: HA-05 ACTIVE UNKNOWN + STANDBY HEALTHY → UNKNOWN
gate: G-RUNTIME
reference: HA
fixture_id: F-HA-05-ACTIVE-UNKNOWN
env_prereq_id: ENV-LAB-01
runner: runners/run_fi_matrix.py
depends_on: [HA-01-001]
timeout: 300
preconditions:
  - "Active source RG UNKNOWN (health probe lost)"
  - "Standby source RG HEALTHY"
actions:
  - step: 1
    do: "Observe failover decision ACTIVE=UNKNOWN / STANDBY=HEALTHY"
expected:
  channel_health: "UNKNOWN"
  standby_not_promoted_despite_healthy: true
pass_rule: "channel_health == 'UNKNOWN' AND standby_not_promoted_despite_healthy == true"
negative_case:
  id: HA-05-001-NEG
  do: "UNKNOWN silently promoted to HEALTHY via standby"
  expect_fail: "violates Rule 6 (reject UNKNOWN, fall to UNKNOWN; do not auto-promote)"
evidence:
  - "evidence/{id}_{run_ts}_ha_state.json"
artifact_naming: "{id}_{run_ts}_{pass|fail}.json"
retry_rule: {max: 0, backoff_s: 0}
abort_rule: {consecutive_fail: 1}
