id: FI-04-001
title: FI-04 Clock Drift +5ms/min → FALLBACK to TIMECODE
gate: G-RUNTIME
reference: FI
fixture_id: F-FI-04-CLOCK-DRIFT
env_prereq_id: ENV-LAB-01
runner: runners/run_fi_matrix.py
depends_on: [AC-01-001]
timeout: 120
preconditions:
  - "PTP/系统时钟 LOCKED"
actions:
  - step: 1
    do: "Inject FI-04: clock drift +5ms/min for >=10min"
expected:
  fallback_triggered: true
  clock_domain_after: "LOCKED"
  drift_after_ms: 10
pass_rule: "fallback_triggered == true AND clock_domain_after == 'LOCKED' AND drift_after_ms < 10"
evidence:
  - "evidence/{id}_{run_ts}_fi_log.json"
artifact_naming: "{id}_{run_ts}_{pass|fail}.json"
retry_rule: {max: 1, backoff_s: 30}
abort_rule: {consecutive_fail: 3}
